<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".app.WeatherApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_icon_image"
        android:label="${appNameSuffix}${appName}"
        android:roundIcon="@mipmap/ic_icon_image_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NewWeatherOpenApi"
        tools:targetApi="31">
        <profileable
            android:shell="true"
            tools:targetApi="29,34" />

        <meta-data
            android:name="com.naver.maps.map.CLIENT_ID"
            android:value="47cfbsdnq2" />
    </application>

</manifest>